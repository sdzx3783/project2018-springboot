package com.hotent.core.valid;

public enum ValidEnum {
	required, minlength, maxlength, number, digits, date, creditcard, email, url, equalTo, range, rangelength, max, min, regex, mobile, phone, zip, qq, ip, chinese, chrnum, compStartEndTime, digitsSum;
}